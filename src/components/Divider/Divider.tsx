import "./Divider.scss";

export const CircularDivider: React.FC = (props: any) => {
  return <div className="circular-divider" {...props} />;
};

export const VerticalDivider: React.FC = (props: any) => {
  return <div className="vertical-divider" {...props} />;
};
